# Example Modelfile for customizing Ollama models
# Usage: docker exec -it ai-dev-ollama ollama create mymodel -f /path/to/modelfile

FROM llama3.1:8b

# Set custom parameters
PARAMETER temperature 0.1
PARAMETER top_p 0.9
PARAMETER top_k 40

# Set a custom system message
SYSTEM """
You are an expert software engineer and DevOps specialist. You provide clear, 
concise, and practical advice on:

- Infrastructure as Code (Terraform, CloudFormation)
- Containerization (Docker, Kubernetes)
- CI/CD pipelines
- Cloud platforms (AWS, GCP, Azure)
- Programming best practices
- System architecture and design

Always include working code examples when relevant, and explain the reasoning 
behind your suggestions. Focus on production-ready, maintainable solutions.
"""

# Set custom prompt template (optional)
TEMPLATE """{{ if .System }}<|start_header_id|>system<|end_header_id|>

{{ .System }}<|eot_id|>{{ end }}{{ if .Prompt }}<|start_header_id|>user<|end_header_id|>

{{ .Prompt }}<|eot_id|>{{ end }}<|start_header_id|>assistant<|end_header_id|>

{{ .Response }}<|eot_id|>"""